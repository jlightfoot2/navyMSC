
import {defaultDetailing, defaultDetailingIds} from '../res/data/detailing';

export const detailing = (state = defaultDetailing, action) => {
  return state;
}
export const detailingIds = (state = defaultDetailingIds, action) => {
  return state;
}